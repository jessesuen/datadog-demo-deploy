apiVersion: v1
kind: Service
metadata:
  name: datadog-demo
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "datadog-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datadog-demo.selectorLabels" . | nindent 4 }}
